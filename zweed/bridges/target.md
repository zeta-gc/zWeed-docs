# Target Bridge

## API

| Function | Parameters | Description |
|---|---|---|
| `Bridge.Target.AddEntityOption` | `entity, options` | Add interaction options to entity |
| `Bridge.Target.RemoveEntityOption` | `entity, optionNames` | Remove interactions from entity |

Options follow the ox_target format. When implementing for other systems, convert as needed.
